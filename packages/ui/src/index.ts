// @azores/ui — visual UI components & design tokens.
// Buttons, inputs, cards, typography, color/spacing/radius tokens.
// Pure presentation: no app state, no side effects, no data fetching.

export { theme, type Theme } from './theme.js';
